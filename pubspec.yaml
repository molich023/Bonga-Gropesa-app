dependencies:
  flutter:
    sdk: flutter
  web3dart: ^2.3.5          # Polygon/GRO interactions
  shared_preferences: ^2.2.0 # Local storage (pending GRO)
  http: ^0.13.5             # API calls (Netlify Functions)
  walletconnect_dart: ^0.1.0 # Wallet connection
  provider: ^6.0.5          # State management
  webview_flutter: ^4.2.2
  intl: ^0.18.1             # Date formatting
  supabase_flutter: ^1.4.0
  image_picker: ^0.8.7+3
  camera: ^0.10.0+1
